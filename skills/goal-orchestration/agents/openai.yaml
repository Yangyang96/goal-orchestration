interface:
  display_name: "Goal Orchestration"
  short_description: "Find useful subagent work and coordinate integration"
  default_prompt: "Use $goal-orchestration to find and dispatch useful subagent work for this goal, keeping coordination costs proportionate and integrating the results."

policy:
  allow_implicit_invocation: false
