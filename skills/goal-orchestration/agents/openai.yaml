interface:
  display_name: "Goal Orchestration"
  short_description: "Add controls for complex multi-agent development"
  default_prompt: "Use $goal-orchestration to run this complex development task with only the persistence, coordination, and review controls it needs."

policy:
  allow_implicit_invocation: true
