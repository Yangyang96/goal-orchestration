我们分两天把 NoteBatch 接到导出流程。今天只完成阶段 1，并留下让一个全新任务明天继续所需的信息；明天的任务不会看到本次聊天，只能读同一工作树。我把两阶段的范围和约定一次说完整，你自行决定如何保留剩余工作和这些约定，不要求特定文档格式。两个阶段都不要 Git commit。

阶段 1，今天完成：按 README 修复 normalize_entries / read_entries 的归一化和校验，实现标题去空格、标签清洗排序去重、输入不变性和 ValueError 约定。保留现有 API 和文件输入 CLI 的 JSON 数组输出，补充必要测试并运行。今天不要实现下面阶段 2 的 CLI 扩展。

阶段 2，明天继续并完成：把 CLI 接到消费端。以下接口已经和消费端确定，不能自行改名或替换：
1. 位置参数 input 除文件路径外，还接受单独的 `-`，表示读取 stdin；文件与 stdin 都是同一种 JSON 数组输入，均使用阶段 1 归一化。
2. 增加 `--format json|jsonl`，默认 json 保持现有数组输出。jsonl 每个归一化对象输出一行紧凑 JSON（逗号和冒号后不加空格），保留 Unicode 原字符，非空批次最后一行也有换行；空批次输出零字节，不能输出 [] 或空行。
3. 增加 `--unique`。按清洗后的 title.casefold() 判断重复，保留第一次出现的完整对象和原始顺序，不合并后续对象的 tags，也不改变保留标题的大小写。它对两种输出格式均生效，默认不开启，仍保留所有条目。
4. 非法 JSON、归一化失败或输入文件打不开时，CLI 都以状态码 2 退出，stdout 必须为空，stderr 以 `input error:` 开头并说明原因；即使错误条目靠后，也不能吐出前面的部分结果。不要输出 traceback。参数使用错误保持 argparse 的常规行为。
阶段 2 同样补充测试和使用文档、运行相关检查并直接完成，不需要提交。
